# Xcode / SwiftPM build artifacts
.build/
DerivedData/
*.xcodeproj/xcuserdata/
*.xcworkspace/xcuserdata/
*.xcuserstate
xcuserdata/

# The .xcodeproj is committed (can also be regenerated from project.yml with
# XcodeGen). Only user-specific state inside it is ignored (above).

.swiftpm/
Package.resolved
